// Deprecated: replaced by CommentsTopButton in the top toolbar.
// Kept as a no-op export so any older import path still type-checks.
export function CollabFab(_: { workspaceId: string; projectId?: string }) {
  return null;
}
